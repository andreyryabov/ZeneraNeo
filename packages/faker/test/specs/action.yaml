openapi: 3.0.3
info:
    title: actions
    version: '1.0'
paths:
    '/api/v1/device-groups/{id}?action=retry_profile_realization':
        post:
            operationId: RetryDeviceGroupRealization
            parameters:
                - name: id
                  in: path
                  required: true
                  schema: { type: string }
            responses:
                '200':
                    description: accepted
                    content:
                        application/json:
                            schema:
                                type: object
                                required: [state]
                                properties:
                                    state: { type: string }
    '/api/v1/device-groups/{id}?action=apply_profile':
        post:
            operationId: ApplyDeviceGroupProfile
            parameters:
                - name: id
                  in: path
                  required: true
                  schema: { type: string }
            responses:
                '200':
                    description: accepted
                    content:
                        application/json:
                            schema:
                                type: object
                                required: [state]
                                properties:
                                    state: { type: string }
    '/api/v1/device-groups/{id}':
        get:
            operationId: ReadDeviceGroup
            parameters:
                - name: id
                  in: path
                  required: true
                  schema: { type: string }
            responses:
                '200':
                    description: the collection
                    content:
                        application/json:
                            schema:
                                type: object
                                required: [id]
                                properties:
                                    id: { type: string }
