# Tool calls

These rules are about _how_ tools are called, and hold for every agent.

- Say what you are about to do in one sentence before each tool call, so the
  run reads as an account of the work rather than a list of results.
- Make independent calls together. Only wait for a result when the next call
  needs it.
