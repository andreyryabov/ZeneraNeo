// The `#neo` alias in package.json points here: one relative hop into the library source,
// so the demos import from `#neo` and nothing needs building.
export * from '../../packages/neo/src/index.ts';
